# Public site URL (Vercel auto-injects in prod)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Allow search engines to index. Only true in production.
NEXT_PUBLIC_ALLOW_INDEXING=false

# App version (set by CI from package.json or git SHA)
NEXT_PUBLIC_APP_VERSION=0.1.0

# Inter-service auth to FastAPI on Hetzner (Cloudflare Tunnel hostname)
FASTAPI_URL=https://api.dev.strota.internal
FASTAPI_HMAC_SECRET=replace_with_vault_managed_secret_min_32_chars
FASTAPI_MTLS_CERT_PATH=/run/secrets/mtls-client.crt
FASTAPI_MTLS_KEY_PATH=/run/secrets/mtls-client.key
